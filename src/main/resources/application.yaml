spring:
  application:
    name: back
  jackson:
    serialization:
      fail-on-empty-beans: false
  output:
    ansi:
      enabled: ALWAYS
logging:
  level:
    com:
      back: debug
server:
  port: 8080